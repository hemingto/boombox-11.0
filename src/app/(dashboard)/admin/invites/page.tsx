/**
 * @fileoverview Admin invites page - manage admin invitations (SUPERADMIN only)
 * @source boombox-10.0/src/app/admin/invites/page.tsx
 * @refactor Migrated to (dashboard)/admin route group with extracted component
 */

'use client';

import { AdminInvitesPage } from '@/components/features/admin/pages';

export default function InvitesPage() {
  return <AdminInvitesPage />;
}
